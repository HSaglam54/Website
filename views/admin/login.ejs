<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - TVG Klimatechnik</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <style>
        body {
            margin: 0;
            padding: 0;
            background-color: #f5f5f5;
            font-family: 'Outfit', sans-serif;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        .login-container {
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
            width: 100%;
        }
        
        .login-box {
            background: white;
            border-radius: 10px;
            box-shadow: 0 5px 30px rgba(0, 51, 102, 0.1);
            width: 100%;
            max-width: 420px;
            padding: 40px;
            text-align: center;
            position: relative;
        }
        
        .login-logo {
            margin-bottom: 30px;
        }
        
        .login-logo img {
            height: 80px;
            width: auto;
        }
        
        .login-form {
            margin-top: 30px;
        }
        
        .login-title {
            color: #003366;
            margin-bottom: 20px;
            font-size: 1.8rem;
            font-weight: 700;
        }
        
        .login-subtitle {
            color: #666;
            font-size: 1rem;
            margin-bottom: 30px;
        }
        
        .form-group {
            margin-bottom: 25px;
            text-align: left;
        }
        
        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: #333;
        }
        
        .form-control {
            width: 100%;
            padding: 14px 15px;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-family: 'Outfit', sans-serif;
            font-size: 1rem;
            transition: all 0.3s;
            box-sizing: border-box;
        }
        
        .form-control:focus {
            border-color: #D4A574;
            outline: none;
            box-shadow: 0 0 0 3px rgba(212, 165, 116, 0.25);
        }
        
        .btn-primary {
            background: #D4A574;
            color: #003366;
            padding: 14px 30px;
            border: none;
            border-radius: 5px;
            font-size: 1rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            cursor: pointer;
            transition: all 0.3s ease;
            width: 100%;
            margin-top: 10px;
        }
        
        .btn-primary:hover {
            background-color: #C19660;
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
        }
        
        .login-footer {
            margin-top: 30px;
            color: #666;
            font-size: 0.85rem;
        }
        
        .alert {
            padding: 15px;
            border-radius: 5px;
            margin-bottom: 20px;
            color: white;
            background-color: #f44336;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }
        
        .alert-icon {
            font-size: 1.2rem;
            margin-right: 10px;
        }
        
        .close-btn {
            background: transparent;
            border: none;
            color: white;
            font-size: 1.2rem;
            cursor: pointer;
            opacity: 0.7;
            transition: opacity 0.3s;
        }
        
        .close-btn:hover {
            opacity: 1;
        }
        
        .remember-me {
            display: flex;
            align-items: center;
            margin-bottom: 20px;
        }
        
        .remember-me input {
            margin-right: 8px;
        }
        
        .forgot-password {
            text-align: right;
            margin-bottom: 20px;
        }
        
        .forgot-password a {
            color: #003366;
            text-decoration: none;
            font-size: 0.9rem;
            transition: color 0.2s;
        }
        
        .forgot-password a:hover {
            color: #D4A574;
            text-decoration: underline;
        }
        
        .decorative-shape {
            position: absolute;
            background-color: #D4A574;
            height: 5px;
            width: 80px;
            top: 0;
            left: 50%;
            transform: translateX(-50%);
            border-radius: 0 0 5px 5px;
        }
        
        @media (max-width: 480px) {
            .login-box {
                padding: 30px 20px;
            }
        }
    </style>
</head>
<body>
    <main class="login-container">
        <div class="login-box">
            <div class="decorative-shape"></div>
            
            <div class="login-logo">
                <img src="/images/TVG_Logo.svg" alt="TVG Klimatechnik Logo">
            </div>
            
            <h2 class="login-title">Admin-Bereich</h2>
            <p class="login-subtitle">Bitte melden Sie sich an, um fortzufahren</p>
            
            <!-- Fehlerprüfung -->
            <% if (typeof error !== 'undefined' && error) { %>
            <div class="alert">
                <div>
                    <span class="alert-icon">⚠️</span>
                    <%= error %>
                </div>
                <button class="close-btn">&times;</button>
            </div>
            <% } %>
            
            <form action="/admin/login" method="POST" class="login-form">
                <div class="form-group">
                    <label for="username">Benutzername</label>
                    <input type="text" id="username" name="username" class="form-control" required autofocus>
                </div>
                
                <div class="form-group">
                    <label for="password">Passwort</label>
                    <input type="password" id="password" name="password" class="form-control" required>
                </div>
                
                <div class="remember-me">
                    <input type="checkbox" id="remember" name="remember" value="1">
                    <label for="remember">Angemeldet bleiben</label>
                </div>
                
                <div class="forgot-password">
                    <a href="/admin/reset-password">Passwort vergessen?</a>
                </div>
                
                <button type="submit" class="btn-primary">
                    <span>Anmelden</span>
                </button>
            </form>
            
            <div class="login-footer">
                &copy; <%= new Date().getFullYear() %> TVG Klimatechnik GmbH - Alle Rechte vorbehalten
            </div>
        </div>
    </main>
    
    <script>
        // Alert schließen
        document.addEventListener('DOMContentLoaded', function() {
            const closeBtn = document.querySelector('.close-btn');
            if (closeBtn) {
                closeBtn.addEventListener('click', function() {
                    this.parentElement.style.display = 'none';
                });
            }
        });
    </script>
</body>
</html>